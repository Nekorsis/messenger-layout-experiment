import { MESSAGES_MOCK } from "../../utils/mocks";

const ChatMessages = () => {
  return (
    <div>
      {MESSAGES_MOCK.map((i) => {
        return null;
      })}
    </div>
  );
};

export default ChatMessages;
